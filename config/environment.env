# 阶段 07 读取本文件，将 KEY=VALUE 写入用户或系统环境（由 config.env 的 ENV_SCOPE 决定）。
# 格式：每行一个变量；# 开头为注释；空行忽略；键名须匹配 ^[A-Z_][A-Z0-9_]*=

# ── 示例（取消注释并填写）────────────────────────────
# MY_API_BASE=https://api.example.com
# DATABASE_URL=postgresql://localhost:5432/app
